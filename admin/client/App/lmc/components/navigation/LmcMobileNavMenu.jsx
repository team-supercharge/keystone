import React from 'react'
import PropTypes from 'prop-types'

const LmcMobileNavMenu = ({ sections }) => {
    return (
        <div>
        </div>
    )
}

LmcMobileNavMenu.propTypes = {
    sections: PropTypes.array.isRequired
}

export default LmcMobileNavMenu